name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      toolchain:
        description: 'Rust toolchain version'
        required: true
        default: 'stable'

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@v1
      with:
        toolchain: nightly-2026-02-18
        components: rustfmt, clippy
        targets: wasm32-unknown-unknown

    - name: Rust Cache
      uses: Swatinem/rust-cache@v2

    - name: Check formatting
      run: cargo fmt --all -- --check

    - name: Dependency layer check
      run: cargo run --package montrs-cli -- agent deps

    - name: Clippy
      run: cargo clippy --workspace -- -D warnings

    - name: Clippy (auth feature)
      run: cargo clippy --package montrs-cli --features auth -- -D warnings

    - name: Run tests
      run: cargo test --workspace

    - name: Build (auth feature)
      run: cargo build --release --package montrs-cli --features auth

    - name: Validate PRDoc
      if: github.event_name == 'pull_request'
      run: |
        PRDOC="prdoc/pr_${{ github.event.pull_request.number }}.prdoc"
        if [ -f "$PRDOC" ]; then
          cargo install changelogger-cli && changelogger prdoc validate
        else
          echo "No prdoc found at $PRDOC"
          echo "PRDoc will be auto-generated by the prdoc workflow."
        fi

    - name: Build
      uses: ./.github/actions/build
      with:
        toolchain: nightly-2026-02-18
