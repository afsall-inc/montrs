name: 'Build'
description: 'Build the project'
runs:
  using: "composite"
  steps:
    - name: Run Build
      shell: bash
      run: |
        set -euo pipefail
        cargo build --workspace --release
