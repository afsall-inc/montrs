# Agent Guide: montrs-proxy

## Core Concepts
Reverse proxy routing `<slug>.localhost` to mapped ports.

## Important Rules
- Routing is deterministic based on configuration.
- SSL/TLS is handled at the proxy level.
- Logging is available for debugging routes.