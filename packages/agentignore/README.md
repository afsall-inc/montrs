# montrs-agentignore

Agent-first file and directory ignore patterns with IDE-specific export.

## CLI Usage

```bash
montrs agent ignore setup
montrs agent ignore check target/
montrs agent ignore export --target opencode
montrs agent ignore export --target cursor
montrs agent ignore export --target trae
```
